---
title: 'Pre-Algebra Workbook 1: Integers & Absolute Value'
pubDate: '2025-12-08'
tags: ['math', 'pre-algebra', 'workbook', 'integers']
---

import MathProblem from '@/components/ui/MathProblem.astro'
import { problems } from '@/data/prealg-workbook-1'

{problems.map((p, idx) => (

  <>
    {idx > 0 && <hr />}
    <MathProblem data={p} />
  </>
))}
